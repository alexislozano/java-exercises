package ex02;

/**
 * This class computes TTC and HT prices depending on
 * the type and the price  of the purchased item.
 * The tax rate is computed from the HT price.
 */
public class TaxCalculator {
	public static double ttc(Item htItem) {
		return 0.0;
	}
	
	public static double ht(Item ttcItem) {
		return 0.0;
	}
}
